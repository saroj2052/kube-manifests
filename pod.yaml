apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    ports:
    - containerPort: 8080
  - name: nginx1
    image: nginx:1.14.2
    ports:
    - containerPort: 80